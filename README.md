# Week_17_Kafka_Python

#### To run the project or rbild the image, use the following command:
`docker compose up --build --force-recreate --no-deps -d`

#### To Shut down Docker Compose and delete the volumes :
`docker compose down -v `



